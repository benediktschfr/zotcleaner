% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zot_journals.R
\name{zot_find_journals}
\alias{zot_find_journals}
\alias{zot_merge_journals}
\title{Handle Duplicate Journals in Zotero}
\usage{
zot_find_journals(con, pattern, ignore_case = TRUE)

zot_merge_journals(con, merge_ids, target_id = NULL)
}
\arguments{
\item{con}{An active DBI connection to a Zotero database.}

\item{pattern}{A character string containing a regular expression to search for.}

\item{ignore_case}{Logical. Should the search be case-insensitive? Default is TRUE.}

\item{merge_ids}{A numeric vector of valueIDs that should be merged.}

\item{target_id}{(Optional) The specific valueID that should be kept. If NULL
and the session is interactive, an interactive selection menu is presented.}
}
\value{
\itemize{
\item \code{zot_find_journals}: A tibble containing the matching journal strings and their valueIDs.
\item \code{zot_merge_journals}: Invisible TRUE if successful, FALSE if cancelled or failed.
}
}
\description{
These functions allow you to systematically find and merge duplicate or
inconsistently formatted journal titles (publicationTitle) in the Zotero database.
}
\examples{
# 1. Create a clean in-memory test environment
mock_db <- zot_mock_db()

# 2. Find all variations of a specific journal (e.g., "Phys")
phys_journals <- zot_find_journals(mock_db, "Phys")
phys_journals

# 3. Merge duplicates programmatically by specifying the target master ID
# (In an interactive session, you can leave target_id = NULL for a menu)
zot_merge_journals(
  con = mock_db,
  merge_ids = phys_journals$valueID,
  target_id = 16
)

# 4. Disconnect safely
zot_disconnect_db(mock_db)

}
