% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zot_authors.R
\name{zot_find_authors}
\alias{zot_find_authors}
\alias{zot_merge_authors}
\title{Handle Duplicate Authors in Zotero}
\usage{
zot_find_authors(con, pattern, ignore_case = TRUE)

zot_merge_authors(con, merge_ids, target_id = NULL)
}
\arguments{
\item{con}{An active DBI connection to a Zotero database.}

\item{pattern}{A character string containing a regular expression to search for.}

\item{ignore_case}{Logical. Should the search be case-insensitive? Default is TRUE.}

\item{merge_ids}{A numeric vector of creatorIDs that should be merged.}

\item{target_id}{(Optional) The specific creatorID that should be kept. If NULL
and the session is interactive, an interactive selection menu is presented.}
}
\value{
\itemize{
\item \code{zot_find_authors}: A tibble containing the matching creators.
\item \code{zot_merge_authors}: Invisible TRUE if successful, FALSE if cancelled or failed.
}
}
\description{
These functions allow you to systematically find and merge duplicate or
inconsistently formatted author entries in the Zotero database.
}
\examples{
# 1. Create a clean in-memory test environment
mock_db <- zot_mock_db()

# 2. Find all variations of a specific author (e.g., "Harrison")
harrison_variations <- zot_find_authors(mock_db, "Harrison")
harrison_variations

# 3. Merge duplicates programmatically by specifying the target master ID
# (In an interactive session, you can leave target_id = NULL for a menu)
zot_merge_authors(
  con = mock_db,
  merge_ids = harrison_variations$creatorID,
  target_id = 3
)

# 4. Disconnect safely
zot_disconnect_db(mock_db)

}
